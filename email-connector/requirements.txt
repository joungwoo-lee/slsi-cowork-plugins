# PST decoding — libpff-python is the correct PyPI name (NOT pypff-python).
# Pin to 20211114 because it is the only version that ships a prebuilt
# Windows wheel (cp39-win_amd64). Newer releases have macOS wheels only,
# so on Windows pip would fall back to sdist → C build → fail.
# Requires: Python 3.9, 64-bit, Windows.
libpff-python==20211114

# Mail body / attachment conversion
markdownify>=0.11.6
striprtf>=0.0.26      # fallback for RTF-only mail bodies (pure Python, no compiler)
pymupdf>=1.23.0
python-docx>=1.1.0

# Storage + API
# Pin qdrant-client to 1.7.0: storage.vector_search uses client.search()
# which returns a flat list of ScoredPoint. From 1.10 onward search() is
# deprecated in favour of query_points() with a different return shape,
# and from 1.15 onward Python 3.9 is no longer supported. Pinning here
# keeps the API contract stable on Python 3.9.
qdrant-client==1.7.0
requests>=2.31.0

# Config from .env (matches retriever_engine project's variable scheme)
python-dotenv>=1.0.0
